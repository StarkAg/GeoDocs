# PDF backend (Puppeteer server). Run with: npm run api
# Next.js API routes proxy to this URL when set.
PDF_BACKEND_URL=http://localhost:3001

# Optional: point frontend directly to backend (e.g. for separate deploy). Leave unset to use Next.js proxy.
# NEXT_PUBLIC_API_URL=http://localhost:3001

# Production example (on VPS):
# PDF_BACKEND_URL=http://localhost:3001
# NEXT_PUBLIC_API_URL=https://your-domain.com
